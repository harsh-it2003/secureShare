<!DOCTYPE html>
<html>

<head>
  <title>Password-Protected File Sharing</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
  <div class="positionIt">
    <div class="container">
      <h1>Password-Protected File Sharing</h1>
      <%if(data.justPassword==0){%>
        <form method="POST" action="/upload" enctype="multipart/form-data">
          <div>
            <label for="file">Select a File:</label>
            <input type="file" id="file" name="file">
          </div>
          <div>
            <label for="password">Enter Password:</label>
            <input type="password" id="password" name="password">
          </div>
          <br>
          <button type="submit">Upload</button>
        </form>
        <%}%>
          <%if(data.justPassword==1){%>
            <form method="POST">
              <div style="margin: 30px 0px 10px 0px;">
                <label for="password">Enter Password:</label>
                <input type="password" id="password" name="password">
              </div>
              <br>
              <button type="submit">send</button>
            </form>
            <%}%>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <script>
    var data = JSON.parse('<%- JSON.stringify(data) %>');
    if (data.justPassword && data.wrongPassword) {
      Swal.fire({
        icon: 'error',
        title: 'Wrong Password',
        text: 'Please enter the correct password.',
        timer: 3000, // Automatically close after 3 seconds
        showConfirmButton: false
      });
    }
  </script>

</body>



</html>